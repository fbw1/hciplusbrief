% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/progress_bubble_hci.R
\name{progress_bubble_hci}
\alias{progress_bubble_hci}
\title{HCI+ progress bubble chart}
\usage{
progress_bubble_hci(
  value = 280,
  progress = 280,
  max_value = 325,
  title = "",
  txt.col = "white",
  radius = 1.7,
  value_size = 9,
  legend_title = NULL,
  legend_text_size = 8,
  legend_title_size = 9,
  legend_key_size = 0.2,
  legend_spacing_x = 0,
  legend_key_width = 0.35
)
}
\arguments{
\item{value}{Numeric. The HCI+ value displayed as a label inside the inner
circle. This is purely the text shown on the plot and does not affect sizing.}

\item{progress}{Numeric. The amount of achieved HCI+ used to scale the inner
bubble relative to \code{max_value}. Values are clamped to \eqn{[0, max_value]}.}

\item{max_value}{Numeric. The maximum possible HCI+ used to normalize
\code{progress}. Defaults to 325.}

\item{title}{Character. Used as the legend title when \code{legend_title} is
\code{NULL}. Defaults to \code{""}.}

\item{txt.col}{Character. Text colour for the \code{value} label.}

\item{radius}{Numeric. Radius of the outer (potential) bubble, in plot units.}

\item{value_size}{Numeric. Font size for the \code{value} label.}

\item{legend_title}{Character or \code{NULL}. Legend title. If \code{NULL},
\code{title} is used.}

\item{legend_text_size}{Numeric. Legend text size.}

\item{legend_title_size}{Numeric. Legend title size.}

\item{legend_key_size}{Numeric. Legend key size (height/size of legend symbols).}

\item{legend_spacing_x}{Numeric. Horizontal spacing between legend items, in cm.}

\item{legend_key_width}{Numeric. Width of the legend key, in cm.}
}
\value{
A \code{ggplot} object.
}
\description{
Creates a two-circle "progress" chart where the outer circle represents the
maximum (potential) HCI+ and the inner circle represents progress toward that
maximum. The inner circle radius is scaled by \code{sqrt(progress / max_value)}
so its \emph{area} is proportional to progress.
}
\details{
The plot draws two filled circles via \code{ggforce::geom_circle()}:
\itemize{
\item \strong{Potential Productivity}: outer bubble with radius \code{radius}.
\item \strong{Actual HCI+}: inner bubble with radius \code{sqrt(progress / max_value) * radius}.
}
This scaling makes bubble \emph{area} proportional to progress, which is often
easier to interpret than scaling radius linearly.
}
\examples{
# Basic usage
progress_bubble_hci(value = 280, progress = 280)

# Show lower progress (inner bubble shrinks)
progress_bubble_hci(value = 120, progress = 120, max_value = 325)

# Custom legend title and styling
progress_bubble_hci(
  value = 280, progress = 280,
  legend_title = "HCI+",
  legend_title_size = 10,
  legend_text_size = 9
)

}
